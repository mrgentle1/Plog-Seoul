import { atom } from "recoil";

export const headerTitleState = atom({
  key: "headerTitleState",
  default: null,
});

export const reviewTitleState = atom({
  key: "reviewTitleState",
  default: null,
});
